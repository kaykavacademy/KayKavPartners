# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# editors
.vscode/
.idea/
*.swp

# node (only ever used for local QA, never for the site itself)
node_modules/
package-lock.json

# video master — 1080p60, way past GitHub's 100MB file limit.
# The web encode (assets/video/kaykav-deb-story.mp4) is what ships.
assets/video/Kaykav DEB Story.mp4
.gstack/

# poster master — the compressed pitch-poster.jpg is what ships.
assets/img/pitch-poster.png
